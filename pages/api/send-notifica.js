const transporter = nodemailer.createTransport({
  service: 'gmail',  // ← 'gmail' non 'smtp.ethereal.email'
  auth: {
    user: process.env.scarpariinside@gmail.com,
    pass: process.env.rmpc kzrh kach fyoi
  }
});
